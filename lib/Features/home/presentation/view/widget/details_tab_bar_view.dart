import 'package:flutter/material.dart';

class DetailsTabBarView extends StatelessWidget {
  const DetailsTabBarView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
